{
  "consul": "localhost:8500",
  "services": [
    {
      "name": "serializer",
      "port": {{.PORT}},
      "health": "/usr/bin/curl -o /dev/null --fail -s http://localhost:8080/heartbeat",
      "poll": 3,
      "ttl": 10
    }
  ],
  "coprocesses": [
    {
      "command": ["/usr/local/bin/consul", "agent",
                  "-data-dir=/data",
                  "-config-dir=/config",
                  "-rejoin",
                  "-retry-join", "{{ if .CONSUL_HOST }}{{ .CONSUL_HOST }}{{ else }}consul{{ end }}",
                  "-retry-max", "10",
                  "-retry-interval", "10s"],
      "restarts": "unlimited"
    }
  ]
}